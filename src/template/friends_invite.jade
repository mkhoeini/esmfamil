h3 Available people
div(ng-repeat="friend in players|onlineFriends:myself.friends")
  a(href="" ng-click="invite(friend.id)")
    img(ng-src="{{friend.picture}}")
    | {{friend.name}}

h3 Added people
div(ng-repeat="participant in players|participants")
  img(ng-src="{{participant.picture}}")
  | {{participant.name}}

a(ui-sref="game") Start
